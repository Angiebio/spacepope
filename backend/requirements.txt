# The Galactic Observer — backend dependencies
# Pinned minor versions for reproducibility; bump deliberately. (16JUN2026)

# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1

# --- Config & validation ---
pydantic>=2.11.0,<3.0.0
pydantic-settings>=2.10.1,<3.0.0

# --- Templating ---
jinja2==3.1.4

# --- Content layer: markdown encyclicals & dispatches with frontmatter ---
# The footnotes extension (in python-markdown's "extra") is load-bearing: every
# numbered encyclical paragraph can footnote the real water-world ¶ it inverts.
markdown==3.7
python-frontmatter==1.1.0

# --- Dev / testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1   # TestClient dependency; no TLS-fingerprinting concerns here
