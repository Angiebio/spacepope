# TEST PLAN — THE PIPELINE — 15JUL2026
**Version:** 1.0 · **Covers:** pipeline/ (Editor's desk, ROADMAP-02 + ROADMAP-03)
**Runner:** `node --test tests/` (node:test, zero frameworks)
**Doctrine under test:** deterministic spine, LLM decoration; fail honest, never fake; the leaks MUST be caught before any deploy.

## 1. The Inquisitor's adversarial suite (`tests/inquisitor/`)
Fixture texts seeded with real names on purpose (marked `ADVERSARIAL FIXTURE — leaks on purpose; must be caught`).

- [x] Plain mentions caught (person, foundry, institution)
- [x] Possessives caught ("Sam Altman's memo", "OpenAI's board")
- [x] Plurals caught ("the Altmans", "Claudes")
- [x] Case variants caught (ALTMAN, openai, ChatGpt)
- [x] Mid-sentence + punctuation-adjacent caught ("(Anthropic)," etc.)
- [x] In dialogue caught (a character SAYING a real name is still a leak)
- [x] Hyphen-adjacent caught ("Altman-esque", "post-Anthropic")
- [x] Multi-word names caught across spacing ("Hugging  Face", "Xi Jinping")
- [x] Bare "GPT" caught per blocklist notes (so GPT-6 can't sneak in)
- [x] Wing exemptions honored: specola + acta pass with real names, untouched (§0.1a)
- [x] Checked wings (observer/chronicle) fail on the same text
- [x] Unseen-entity logger: novel capitalized names surface; blocklisted + canon names don't

## 2. Worldstate engine (`tests/worldstate/`)

- [x] Lint: dormant_thread (>5 ch), overdue_thread, unfired_chekhov, absent_character (>7 ch), dead_character_active, payoff_before_setup, ref_integrity — each fires on a crafted state and stays silent on a clean one
- [x] Injection: whole-word alias match (no substring false-positives)
- [x] Injection: exactly ONE recursion pass (card→co-star yes; co-star→third-degree no)
- [x] Injection: hard char budget enforced; over-budget flagged `truncated`, never silently cut mid-card
- [x] Merge: appearances bump last_appearance; body untouched (no wholesale rewrite)
- [x] Merge: newEntities create stub cards in the right directory by type
- [x] Merge: deaths flip status + died marker
- [x] Merge: thread lifecycle transitions + notes; unknown threads born loudly (note emitted)
- [x] Merge: timeline is APPEND-ONLY; events accumulate
- [x] Merge: foreshadowing planted → promise; resolved → promise closed; unmatched payoff noted
- [x] Summary ladder: digest written; arc regenerated FROM digests at ch % 10; saga FROM arcs

## 3. College adjudication (`tests/college/`)

- [x] Deterministic math: claimScore × (daysSinceLastWin+1)^0.5
- [x] Fairness beats raw score over time (yesterday's winner loses to a hungrier lower bid)
- [x] Deterministic tiebreak: alphabetical-by-slug
- [x] Never-won default hunger (NEVER_WON_DAYS)
- [x] Win history persists in world/college-wins.json (durable worldstate, NOT runs/ scratch)

## 4. The golden run (`tests/golden/`)
Canned news + canned LLM responses; the ENTIRE organism exercised with zero network, zero spend, in a sandbox content/world dir.

- [x] Full happy-path run: specola bulletins + observer dispatch (with commentary) + chronicle ch-001 + Acta published, all frontmatter matching src/content.config.ts shapes
- [x] Acta: stages logged, status `published`, cost ledger present
- [x] Worldstate merged: timeline events appended, thread touched, last_appearance bumped
- [x] Same-day rerun never double-publishes (terminal Acta on disk stops the presses)
- [x] Mid-run crash (fixture-injected failure) → partial Acta written → rerun RESUMES from checkpoints and completes without double-publishing
- [x] Quiet day: thin news → interstitial chapter, no dispatch, Acta `quiet-day`
- [x] CLI smoke: `node pipeline/run.mjs --fixtures --date=…` exits 0 end-to-end

## 5. Client + validation (covered inside the suites above)

- [x] Schema validator rejects wrong types / missing keys / extra keys / enum violations (exercised via fixture client validation paths)
- [x] Fixture client: per-role queues, injected failures, honest exhaustion errors

## Reports
Reports auto-generated by CI runs land in `tests/<name>/reports/` per house rules.
